<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tvarkarastis">

    <application
        android:allowBackup="true"
        android:icon="@drawable/jbg_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.Main"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Schedule" />
        <activity android:name=".activities.ChooseClass" />
        <activity
            android:name=".activities.ChooseStudent"
            android:label="@string/title_activity_choose_student"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".activities.ChooseClassLetter10"
            android:label="@string/title_activity_choose_class_letter" />
        <activity
            android:name=".activities.ChooseClassLetter9"
            android:label="@string/title_activity_choose_class_letter9" />
        <activity
            android:name=".activities.ScheduleTime"
            android:label="@string/title_activity_schedule_time"></activity>
    </application>

</manifest>
